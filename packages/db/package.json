{
  "name": "@personal-blog/db",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/client.js",
  "scripts": {
    "build": "echo \"db package uses source directly\"",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:seed": "node src/seed.js",
    "test": "echo \"No db tests yet\""
  },
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "prisma": "^6.5.0"
  }
}
